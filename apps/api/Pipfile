[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.109.0"
uvicorn = {extras = ["standard"], version = ">=0.27.0"}
python-multipart = ">=0.0.6"
pydantic = ">=2.5.0"
openai-whisper = ">=20231117"
pydub = ">=0.25.1"
spacy = ">=3.7.0"
numpy = ">=1.26.0"
torch = ">=2.1.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-asyncio = ">=0.23.0"
httpx = ">=0.26.0"
black = ">=23.12.0"
ruff = "*"

[requires]
python_version = "3.10"

[scripts]
dev = "uvicorn src.main:app --reload --port 8000"
start = "uvicorn src.main:app --port 8000"
lint = "ruff check src/"
lint-fix = "ruff check --fix --unsafe-fixes src/"
format = "black src/"
format-check = "black --check src/"
