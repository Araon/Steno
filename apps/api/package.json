{
  "name": "@steno/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "./venv/bin/python -m uvicorn src.main:app --reload --port 8000",
    "start": "./venv/bin/python -m uvicorn src.main:app --port 8000",
    "install:py": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  }
}
